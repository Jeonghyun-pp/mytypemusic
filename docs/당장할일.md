# 당장할일 — 소규모 팀 도구 안정화 실행 계획

> 현재 코드 상태 기반, 구체적 파일/줄 단위 수정 계획
> 우선순위: 안정성 → 가시성 → 구조 개선 → 인프라

---

## Sprint 1: 안정성 확보 (1~2주)

### 1-1. 공통 fetchWithTimeout 유틸 생성

**신규 파일**: `src/lib/fetch-utils.ts`

```typescript
// 모든 외부 API 호출에서 재사용할 timeout fetch
export async function fetchWithTimeout(
  url: string,
  opts: RequestInit & { timeout?: number } = {}
): Promise<Response> {
  const { timeout = 10_000, ...fetchOpts } = opts;
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), timeout);
  try {
    return await fetch(url, { ...fetchOpts, signal: controller.signal });
  } finally {
    clearTimeout(timer);
  }
}
```

### 1-2. 트렌드 provider별 timeout 적용

**수정할 파일 6개:**

| 파일 | 줄수 | 현재 | 변경 |
|------|------|------|------|
| `trends/youtube.ts` | 49줄 | `fetch(url)` (line ~20) | `fetchWithTimeout(url, { timeout: 10_000 })` |
| `trends/google.ts` | 39줄 | `googleTrends.dailyTrends()` (line ~16) | `Promise.race([googleTrends.dailyTrends(), timeout(10_000)])` |
| `trends/hackernews.ts` | 42줄 | `fetch(url)` (line ~13, ~19) | `fetchWithTimeout(url, { timeout: 8_000 })` |
| `trends/naver-datalab.ts` | 75줄 | `fetch(url, { method: "POST" })` (line ~30) | `fetchWithTimeout(url, { method: "POST", timeout: 10_000 })` |
| `trends/naver-search.ts` | 101줄 | `fetch(newsUrl)`, `fetch(blogUrl)` (line ~41, ~68) | 각각 `fetchWithTimeout(url, { timeout: 8_000 })` |
| `pipeline/web-search.ts` | 178줄 | `fetch()` 3곳 (line ~62, ~104, ~147) | 각각 `fetchWithTimeout(url, { timeout: 15_000 })` |

**작업 순서:**
1. `src/lib/fetch-utils.ts` 생성 (10줄)
2. 각 trends provider에서 import 후 fetch → fetchWithTimeout 교체
3. web-search.ts에서도 동일 교체
4. google.ts는 npm 모듈이므로 Promise.race로 감싸기

**검증:** `npm run build` 통과 확인

---

### 1-3. LLM Vision 타임아웃 조정

**수정 파일**: `src/lib/llm.ts` (212줄)

```
현재 (line ~178): timeout: 60_000  ← Vercel 함수 한도와 동일, 경합 발생
변경:             timeout: 50_000  ← 10초 여유 확보
```

단순 상수 변경 1줄.

---

### 1-4. Job 실패 알림 시스템

**신규 파일**: `src/lib/notify.ts`

```typescript
export async function notifySlack(message: string, context?: Record<string, unknown>) {
  const url = process.env.SLACK_WEBHOOK_URL;
  if (!url) return;
  await fetch(url, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      text: message,
      blocks: context ? [
        { type: "section", text: { type: "mrkdwn", text: message } },
        { type: "section", text: { type: "mrkdwn", text: "```" + JSON.stringify(context, null, 2) + "```" } },
      ] : undefined,
    }),
  }).catch(() => {}); // 알림 실패가 본 로직을 막으면 안 됨
}
```

**수정 파일 1**: `src/lib/jobs/queue.ts` (144줄)

```
수정 위치: line ~123 (재시도 초과 후 failed 처리 블록)

현재:
  await prisma.job.update({
    where: { id: job.id },
    data: { status: "failed", error: errMsg },
  });

추가:
  await notifySlack(
    `[Job 최종 실패] ${job.type}`,
    { jobId: job.id, error: errMsg, retryCount: job.retryCount }
  );
```

**수정 파일 2**: `src/lib/autopilot/scanner.ts` (168줄)

```
수정 위치 1: line ~90 (generateProposals catch 블록)

현재:
  } catch {
    // 빈 catch — 에러 무시
  }

변경:
  } catch (err) {
    console.error(`[autopilot] 제안 생성 실패 (config: ${configId}):`, err);
    await notifySlack(
      `[Autopilot] 제안 생성 실패`,
      { configId, platform, error: String(err) }
    );
  }

수정 위치 2: line ~161 (publishApprovedProposals catch 블록)

현재:
  } catch {
    // 발행 실패 무시
  }

변경:
  } catch (err) {
    console.error(`[autopilot] 발행 실패:`, err);
    await notifySlack(
      `[Autopilot] 게시물 발행 실패`,
      { proposalId: proposal.id, error: String(err) }
    );
  }
```

**사전 준비:** Slack workspace에서 Incoming Webhook URL 생성 → `.env`에 `SLACK_WEBHOOK_URL` 추가

**검증:**
- 의도적으로 잘못된 API 키로 autopilot 실행 → Slack 알림 수신 확인
- Job 3회 실패 시나리오 → Slack 알림 확인

---

### 1-5. LLM 비용 추적

**Prisma 스키마 추가** (`prisma/schema.prisma`):

```prisma
model LlmUsageLog {
  id               String   @id @default(cuid())
  model            String
  promptTokens     Int
  completionTokens Int
  totalTokens      Int
  costUsd          Float
  caller           String   // autopilot, pipeline, suggestions, topic-intelligence
  durationMs       Int?
  createdAt        DateTime @default(now())

  @@index([createdAt])
  @@index([caller])
  @@index([model])
}
```

**수정 파일**: `src/lib/llm.ts` (212줄)

```
수정 위치: callGpt() 함수 내부 (line ~46), 응답 반환 전

현재:
  const text = res.choices[0]?.message?.content ?? "";
  return text;

추가:
  // 비용 추적 (fire-and-forget)
  if (res.usage) {
    trackLlmUsage(opts?.model ?? "gpt-4o-mini", res.usage, opts?.caller, elapsed).catch(() => {});
  }
  return text;
```

**신규 함수** (llm.ts 하단 또는 별도 파일):

```typescript
const MODEL_RATES: Record<string, { input: number; output: number }> = {
  "gpt-4o-mini":    { input: 0.15e-6,  output: 0.60e-6 },
  "gpt-4o":         { input: 2.50e-6,  output: 10.0e-6 },
  "gpt-4.1-mini":   { input: 0.40e-6,  output: 1.60e-6 },
  "gpt-4.1":        { input: 2.00e-6,  output: 8.00e-6 },
};

async function trackLlmUsage(
  model: string,
  usage: { prompt_tokens: number; completion_tokens: number; total_tokens: number },
  caller?: string,
  durationMs?: number
) {
  const rate = MODEL_RATES[model] ?? MODEL_RATES["gpt-4o-mini"];
  const cost = usage.prompt_tokens * rate.input + usage.completion_tokens * rate.output;
  await prisma.llmUsageLog.create({
    data: {
      model,
      promptTokens: usage.prompt_tokens,
      completionTokens: usage.completion_tokens,
      totalTokens: usage.total_tokens,
      costUsd: Math.round(cost * 1e6) / 1e6, // 소수점 6자리
      caller: caller ?? "unknown",
      durationMs,
    },
  });
}
```

**호출부 caller 태깅** (각 파일에서 opts에 caller 추가):

| 호출 위치 | caller 값 |
|-----------|----------|
| `scanner.ts` → `callGptJson(...)` | `"autopilot"` |
| `topic-intelligence.ts` → `callGptJson(...)` | `"topic-intelligence"` |
| `suggestions/route.ts` → `callGptJson(...)` | `"suggestions"` |
| `orchestrator.ts` → `callGpt(...)` | `"pipeline"` |
| `design-director.ts` → `callGptJson(...)` | `"design"` |

**작업 순서:**
1. Prisma 스키마에 `LlmUsageLog` 추가
2. `npx prisma migrate dev --name add-llm-usage-log`
3. `llm.ts`에 `trackLlmUsage` 함수 + 호출 삽입
4. 주요 호출부에 `caller` 옵션 추가 (5~6개 파일)

**검증:** 파이프라인 1회 실행 후 `SELECT model, caller, SUM(costUsd) FROM LlmUsageLog GROUP BY model, caller` 확인

---

### Sprint 1 체크리스트

```
[ ] fetch-utils.ts 생성
[ ] trends 6개 provider에 timeout 적용
[ ] web-search.ts 3곳 timeout 적용
[ ] llm.ts Vision timeout 60s → 50s
[ ] notify.ts 생성
[ ] .env에 SLACK_WEBHOOK_URL 추가
[ ] queue.ts 최종 실패 시 알림 추가
[ ] scanner.ts catch 블록 2곳 알림 추가
[ ] Prisma에 LlmUsageLog 모델 추가
[ ] migrate dev 실행
[ ] llm.ts에 trackLlmUsage 삽입
[ ] 주요 호출부에 caller 옵션 추가
[ ] 전체 빌드 확인 (npm run build)
[ ] 수동 테스트: autopilot 1회 실행 → Slack 알림 + LLM 비용 기록 확인
```

---

## Sprint 2: 가시성 확보 (2~3주차)

### 2-1. 구조화된 로깅 (Pino)

**설치:**
```bash
npm install pino
```

**신규 파일**: `src/lib/logger.ts`

```typescript
import pino from "pino";

export const logger = pino({
  level: process.env.LOG_LEVEL ?? "info",
  formatters: {
    level: (label) => ({ level: label }),
  },
});

// 컨텍스트 포함 child logger 생성 헬퍼
export function createLogger(context: Record<string, unknown>) {
  return logger.child(context);
}
```

**교체 대상 (우선순위순):**

| 파일 | console 호출 수 | 교체 방법 |
|------|----------------|----------|
| `llm.ts` | 4 (warn, error) | `logger.warn({ attempt, model }, "LLM retry")` |
| `queue.ts` | 3 (log, error) | `createLogger({ jobId, type }).info("processing")` |
| `scanner.ts` | 3 (error) | `createLogger({ configId }).error(err, "proposal failed")` |
| `studio/http.ts` | 1 (error) | `logger.error(err, "server error")` |
| `trends/index.ts` | 0 (silent) | 실패 시 `logger.warn({ source }, "trend fetch failed")` 추가 |
| 기타 handlers | ~10 | 점진적 교체 |

**1차 범위:** 핵심 파일 5개만 먼저 교체 (llm, queue, scanner, http, trends/index)
**2차 범위:** 나머지 handlers, API routes 점진적 교체

**검증:** Vercel 배포 후 로그 출력이 JSON 형식인지 확인

---

### 2-2. Sentry 에러 추적

**설치:**
```bash
npx @sentry/wizard@latest -i nextjs
# → sentry.client.config.ts, sentry.server.config.ts 자동 생성
# → next.config.ts에 withSentryConfig 래퍼 추가
```

**수정 파일**: `src/lib/studio/http.ts` (24줄)

```
현재:
  export function serverError(error: unknown) {
    console.error(error);
    return NextResponse.json({ error: "Internal server error" }, { status: 500 });
  }

변경:
  import * as Sentry from "@sentry/nextjs";

  export function serverError(error: unknown) {
    Sentry.captureException(error);
    logger.error(error, "server error");
    return NextResponse.json({ error: "Internal server error" }, { status: 500 });
  }
```

**추가 계측 (llm.ts):**
```
최종 실패 시 (line ~107):
  Sentry.captureException(lastError, {
    tags: { model, caller: opts?.caller },
    extra: { attempts: RETRY_DELAYS.length + 1 },
  });
```

**비용:** 무료 (5K 이벤트/월), DSN을 `.env`에 `SENTRY_DSN`으로 추가

**검증:** 의도적 에러 발생 → Sentry 대시보드에서 이벤트 확인

---

### Sprint 2 체크리스트

```
[ ] pino 설치
[ ] logger.ts 생성
[ ] 핵심 파일 5개 console → logger 교체
[ ] Sentry wizard 실행
[ ] http.ts에 Sentry.captureException 추가
[ ] llm.ts 최종 실패에 Sentry 태깅
[ ] .env에 SENTRY_DSN 추가
[ ] 배포 후 Sentry 대시보드 확인
[ ] Vercel Log Drain → Axiom 연결 (선택)
```

---

## Sprint 3: 구조 개선 (3~4주차)

### 3-1. N+1 쿼리 수정

**파일 1**: `src/lib/autopilot/scanner.ts` — `publishApprovedProposals()`

```
현재 (line ~117~167):
  proposals = findMany({ status: "approved" })
  for (proposal of proposals) {
    config = findUnique({ id: proposal.configId })     // N+1
    publication = create({ ... })
    publishNow(publication.id)                          // 내부에서 또 조회
  }

변경:
  proposals = findMany({
    where: { status: "approved" },
    include: {
      config: { include: { snsAccount: true } },       // JOIN 1회
    },
    take: 5,
  })
  for (proposal of proposals) {
    // proposal.config 바로 사용, 추가 쿼리 없음
    publication = create({ ... })
    // publishNow에 config/account 전달하여 내부 조회 제거
  }
```

**파일 2**: `src/lib/jobs/handlers/analyticsCollect.ts` (123줄)

```
현재 (line ~26~88):
  for (account of accounts) {
    existing = findUnique(...)        // N회
    prevSnapshot = findUnique(...)    // N회
    todayPubs = findMany(...)         // N회, 바운드 없음
    todayPerf = findMany(...)         // N회, 바운드 없음
  }
  // 계정 10개 → ~40 쿼리

변경:
  accountIds = accounts.map(a => a.id)
  [allSnapshots, allPubs, allPerf] = await Promise.all([
    prisma.analyticsSnapshot.findMany({
      where: { snsAccountId: { in: accountIds }, date: { in: [today, yesterday] } }
    }),
    prisma.publication.findMany({
      where: { snsAccountId: { in: accountIds }, createdAt: { gte: todayStart } },
      take: 200,                     // 바운드 추가
    }),
    prisma.postPerformance.findMany({
      where: { snsAccountId: { in: accountIds }, collectedAt: { gte: todayStart } },
      take: 500,
    }),
  ])
  // 그룹핑
  snapsByAccount = groupBy(allSnapshots, "snsAccountId")
  pubsByAccount = groupBy(allPubs, "snsAccountId")
  perfByAccount = groupBy(allPerf, "snsAccountId")
  // 계정 10개 → 3 쿼리
```

**파일 3**: `src/lib/jobs/handlers/commentFetch.ts` (94줄)

```
현재 (line ~63):
  for (comment of comments) {
    await prisma.incomingMessage.create({ data: comment })  // N회
  }

변경:
  const allComments = []
  for (pub of publications) {
    const comments = await adapter.fetchCommentsOnPost(pub.externalId)
    allComments.push(...comments.map(c => ({ ...c, publicationId: pub.id })))
  }
  await prisma.incomingMessage.createMany({
    data: allComments,
    skipDuplicates: true,
  })
  // 댓글 100개 → create 1회
```

**검증:** 각 handler 실행 전후 DB 쿼리 수 비교 (Prisma query logging 활성화)

---

### 3-2. 팀 인증 도입 (Clerk)

**설치:**
```bash
npm install @clerk/nextjs
```

**환경변수 추가** (`.env`):
```
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_...
CLERK_SECRET_KEY=sk_...
```

**신규 파일**: `src/middleware.ts` (현재 없음)
```typescript
import { clerkMiddleware } from "@clerk/nextjs/server";
export default clerkMiddleware();
export const config = {
  matcher: [
    "/((?!_next|favicon.ico|api/webhooks|api/inngest).*)",
  ],
};
```

**수정 파일**: `src/app/layout.tsx`
```
추가: <ClerkProvider> 래핑
```

**삭제 대상:**
- `src/app/api/auth/login/route.ts` (26줄) — Clerk가 대체
- `.env`에서 `SITE_PASSWORD` 제거
- 각 route의 `site-auth` 쿠키 체크 로직 제거

**Clerk 대시보드에서:**
- Google OAuth provider 활성화
- 허용 이메일 도메인/목록 설정
- 팀원 초대

**검증:** 로그인 → Clerk 대시보드에서 사용자 활동 확인

---

### Sprint 3 체크리스트

```
[ ] scanner.ts publishApprovedProposals N+1 수정
[ ] analyticsCollect.ts 배치 조회 + groupBy 전환
[ ] commentFetch.ts createMany 전환
[ ] Prisma query logging으로 쿼리 수 감소 확인
[ ] Clerk 설치 + 환경변수 설정
[ ] middleware.ts 생성
[ ] layout.tsx에 ClerkProvider 추가
[ ] 기존 auth/login route 삭제
[ ] site-auth 쿠키 체크 로직 제거
[ ] 팀원 초대 + 로그인 테스트
```

---

## Sprint 4: 인프라 보강 (5~6주차)

### 4-1. Inngest 도입 (Job Queue 교체)

**설치:**
```bash
npm install inngest
```

**신규 파일 구조:**
```
src/lib/inngest/
  client.ts          ← Inngest 클라이언트
  functions/
    autopilot.ts     ← autopilot_scan 대체
    analytics.ts     ← analytics_collect 대체
    comments.ts      ← comment_fetch + comment_monitor 대체
    publish.ts       ← publish 대체
    keywords.ts      ← keyword_scan + keyword_comment_post 대체
    persona.ts       ← persona_learn 대체

src/app/api/inngest/
  route.ts           ← Inngest serve 엔드포인트
```

**전환 순서 (위험도 낮은 것부터):**

```
1단계: analytics_collect → inngest function
  • 현재: queue.ts에서 enqueue → processJobs에서 실행
  • 변경: inngest.createFunction({ cron: "0 16 * * *" })
  • 검증: 대시보드에서 실행 로그 확인

2단계: comment_fetch → inngest function
  • 현재: cron 트리거 → handler
  • 변경: inngest.createFunction({ cron: "*/10 * * * *" })

3단계: autopilot_scan → inngest function (핵심)
  • 현재: scanner.ts의 generateProposals + publishApprovedProposals
  • 변경: step.run()으로 단계별 분리, 각 step 자동 재시도

4단계: publish → inngest function
  • 이벤트 트리거: inngest.send("publication/scheduled")

5단계 (최종): queue.ts + jobs/process/route.ts 삭제
```

**삭제 가능한 코드:**

| 파일 | 줄수 | 상태 |
|------|------|------|
| `jobs/queue.ts` | 144줄 | 전체 삭제 |
| `jobs/types.ts` | ~20줄 | 전체 삭제 |
| `api/jobs/process/route.ts` | 59줄 | Inngest route로 대체 |
| `vercel.json` cron 설정 | 해당 줄 | Inngest cron으로 대체 |
| 각 handler의 retry/timeout 로직 | 핸들러당 ~15줄 | Inngest 내장 기능으로 대체 |

**총 삭제 예상:** ~250줄+

---

### 4-2. 클라우드 스토리지 전환 (Cloudflare R2)

**설치:**
```bash
npm install @aws-sdk/client-s3 @aws-sdk/s3-request-presigner
```

**환경변수 추가:**
```
R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
R2_ACCESS_KEY=...
R2_SECRET_KEY=...
R2_BUCKET=web-magazine-studio
R2_PUBLIC_URL=https://assets.yourdomain.com
```

**신규 파일**: `src/lib/storage.ts`
```typescript
// S3 호환 클라이언트 설정
// uploadFile(key, buffer, contentType) → public URL
// getSignedUrl(key) → 1시간 유효 signed URL
// deleteFile(key)
```

**수정 파일 1**: `src/app/api/reels/upload/route.ts` (33줄)

```
현재:
  const uploadDir = path.resolve(process.cwd(), "../../outputs/reels/uploads");
  await fs.writeFile(path.join(uploadDir, filename), buffer);
  return { url: `/api/reels/file?name=${filename}` };

변경:
  if (file.size > 50 * 1024 * 1024) return badRequest("50MB 초과");
  const key = `reels/${Date.now()}-${crypto.randomUUID()}${ext}`;
  const url = await uploadFile(key, buffer, file.type);
  return { url };
```

**수정 파일 2**: `src/app/api/reels/file/route.ts` (42줄)

```
현재: 로컬 파일 읽어서 응답
변경: R2 signed URL로 리다이렉트 또는 R2 public URL 반환
```

**Cloudflare R2 설정:**
- Cloudflare 대시보드 > R2 > 버킷 생성
- API Token 생성 (S3 호환)
- 커스텀 도메인 연결 (선택)

**검증:** 업로드 → R2 대시보드에서 파일 확인 → URL로 접근 가능 확인

---

### 4-3. CI/CD 기본 설정

**신규 파일**: `.github/workflows/ci.yml`

```yaml
name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Prisma generate
        run: npx prisma generate
        working-directory: apps/studio

      - name: Type check
        run: npx tsc --noEmit
        working-directory: apps/studio

      - name: Lint
        run: npx next lint
        working-directory: apps/studio

      - name: Test
        run: npx vitest run --passWithNoTests
        working-directory: apps/studio

      - name: Prisma validate
        run: npx prisma validate
        working-directory: apps/studio
```

**검증:** PR 생성 → GitHub Actions 통과 확인

---

### Sprint 4 체크리스트

```
[ ] inngest 설치
[ ] client.ts + 첫 function (analytics) 생성
[ ] api/inngest/route.ts 생성
[ ] analytics → inngest 전환 + 검증
[ ] comment_fetch → inngest 전환
[ ] autopilot_scan → inngest 전환
[ ] publish → inngest 전환
[ ] queue.ts, jobs/process 삭제
[ ] @aws-sdk/client-s3 설치
[ ] storage.ts 생성
[ ] R2 버킷 생성 + 환경변수 설정
[ ] upload/file route 수정
[ ] .github/workflows/ci.yml 생성
[ ] PR 생성하여 CI 통과 확인
```

---

## 전체 타임라인 요약

```
Week 1-2  [Sprint 1: 안정성]
  ✦ fetchWithTimeout 유틸 + 전체 provider 적용
  ✦ Slack 장애 알림
  ✦ LLM 비용 추적 테이블 + 기록

Week 3    [Sprint 2: 가시성]
  ✦ Pino 구조화 로깅
  ✦ Sentry 에러 추적

Week 4    [Sprint 3: 구조 개선]
  ✦ N+1 쿼리 3개 파일 수정
  ✦ Clerk 팀 인증

Week 5-6  [Sprint 4: 인프라]
  ✦ Inngest Job Queue 교체
  ✦ R2 클라우드 스토리지
  ✦ GitHub Actions CI
```

---

## 수정 대상 파일 전체 목록

| 파일 | Sprint | 작업 |
|------|--------|------|
| `src/lib/fetch-utils.ts` | 1 | **신규** — 공통 timeout fetch |
| `src/lib/notify.ts` | 1 | **신규** — Slack 알림 유틸 |
| `src/lib/logger.ts` | 2 | **신규** — Pino 구조화 로거 |
| `src/lib/storage.ts` | 4 | **신규** — R2 스토리지 헬퍼 |
| `src/lib/inngest/client.ts` | 4 | **신규** — Inngest 클라이언트 |
| `src/lib/inngest/functions/*.ts` | 4 | **신규** — Inngest functions (5~6개) |
| `src/app/api/inngest/route.ts` | 4 | **신규** — Inngest 서빙 |
| `src/middleware.ts` | 3 | **신규** — Clerk 미들웨어 |
| `.github/workflows/ci.yml` | 4 | **신규** — CI 파이프라인 |
| `src/lib/llm.ts` | 1+2 | **수정** — timeout 조정 + 비용 추적 + Sentry + logger |
| `src/lib/trends/youtube.ts` | 1 | **수정** — timeout 적용 |
| `src/lib/trends/google.ts` | 1 | **수정** — timeout 적용 |
| `src/lib/trends/hackernews.ts` | 1 | **수정** — timeout 적용 |
| `src/lib/trends/naver-datalab.ts` | 1 | **수정** — timeout 적용 |
| `src/lib/trends/naver-search.ts` | 1 | **수정** — timeout 적용 |
| `src/lib/trends/index.ts` | 2 | **수정** — logger 추가 |
| `src/lib/pipeline/web-search.ts` | 1 | **수정** — timeout 적용 |
| `src/lib/jobs/queue.ts` | 1→4 | **수정** → **삭제** (Inngest 전환 후) |
| `src/lib/autopilot/scanner.ts` | 1+3 | **수정** — 알림 + N+1 수정 |
| `src/lib/studio/http.ts` | 2 | **수정** — Sentry + logger |
| `src/lib/jobs/handlers/analyticsCollect.ts` | 3 | **수정** — 배치 쿼리 전환 |
| `src/lib/jobs/handlers/commentFetch.ts` | 3 | **수정** — createMany 전환 |
| `src/app/api/reels/upload/route.ts` | 4 | **수정** — R2 업로드 |
| `src/app/api/reels/file/route.ts` | 4 | **수정** — R2 서빙 |
| `src/app/api/jobs/process/route.ts` | 4 | **삭제** — Inngest 대체 |
| `src/app/api/auth/login/route.ts` | 3 | **삭제** — Clerk 대체 |
| `src/app/layout.tsx` | 3 | **수정** — ClerkProvider 추가 |
| `prisma/schema.prisma` | 1 | **수정** — LlmUsageLog 추가 |
| `vercel.json` | 4 | **수정** — cron 제거 (Inngest 대체) |
| `.env` | 1~4 | **수정** — SLACK_WEBHOOK_URL, SENTRY_DSN, CLERK keys, R2 keys 추가 |

---

## 환경변수 추가 목록

| 변수 | Sprint | 용도 | 비용 |
|------|--------|------|------|
| `SLACK_WEBHOOK_URL` | 1 | 장애 알림 | 무료 |
| `SENTRY_DSN` | 2 | 에러 추적 | 무료 (5K/월) |
| `LOG_LEVEL` | 2 | 로그 레벨 (info/debug/warn) | - |
| `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` | 3 | 팀 인증 | 무료 (10K MAU) |
| `CLERK_SECRET_KEY` | 3 | 팀 인증 | 무료 |
| `INNGEST_EVENT_KEY` | 4 | Job Queue | 무료 (25K step/월) |
| `INNGEST_SIGNING_KEY` | 4 | Job Queue | 무료 |
| `R2_ENDPOINT` | 4 | 파일 스토리지 | 무료 (10GB) |
| `R2_ACCESS_KEY` | 4 | 파일 스토리지 | 무료 |
| `R2_SECRET_KEY` | 4 | 파일 스토리지 | 무료 |
| `R2_BUCKET` | 4 | 파일 스토리지 | 무료 |
| `R2_PUBLIC_URL` | 4 | 파일 스토리지 | 무료 |

**총 추가 비용: 월 $0** (모든 서비스 무료 티어 범위 내)
