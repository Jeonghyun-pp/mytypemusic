# 수동 작업 목록

> 코드 변경 외에 직접 수행해야 하는 작업들

---

## Sprint 1: 안정성 확보

### DB 마이그레이션
- [ ] `cd apps/studio && npx prisma migrate dev --name add-llm-usage-log`
- LlmUsageLog 테이블 생성 (DB 연결 필요)

### 환경변수 설정
- [ ] `.env`에 `SLACK_WEBHOOK_URL` 추가
  - Slack workspace > 앱 관리 > Incoming Webhook 생성
  - 알림 받을 채널 선택 후 URL 복사

### 검증
- [ ] 의도적으로 잘못된 API 키로 autopilot 실행 → Slack 알림 수신 확인
- [ ] Job 3회 실패 시나리오 → Slack 알림 확인
- [ ] 파이프라인 1회 실행 후 `SELECT model, caller, SUM("costUsd") FROM llm_usage_logs GROUP BY model, caller` 확인

---

## Sprint 2: 가시성 확보

### 패키지 설치 (완료)
- [x] `pino` 설치 완료
- [x] `@sentry/nextjs` 설치 완료

### Sentry 초기화 (필수)
- [ ] Sentry wizard 실행: `cd apps/studio && npx @sentry/wizard@latest -i nextjs`
  - sentry.client.config.ts, sentry.server.config.ts 자동 생성됨
  - next.config.ts에 withSentryConfig 래퍼 자동 추가됨
  - 코드에 이미 `@sentry/nextjs` import + captureException 추가되어 있음
  - wizard 실행 전까지는 Sentry가 no-op (DSN 없으면 아무 동작 안 함)

### 환경변수 설정
- [ ] `.env`에 `SENTRY_DSN` 추가
  - Sentry 프로젝트 생성 후 DSN 복사
- [ ] `.env`에 `LOG_LEVEL` 추가 (선택, 기본값: info)

### 검증
- [ ] Vercel 배포 후 로그 출력이 JSON 형식인지 확인
- [ ] 의도적 에러 발생 → Sentry 대시보드에서 이벤트 확인

---

## Sprint 3: 구조 개선

### DB 마이그레이션
- [ ] `cd apps/studio && npx prisma migrate dev --name add-autopilot-relation`
  - AutopilotProposal → AutopilotConfig relation 추가 (스키마만 변경, 기존 데이터 호환)

### Clerk 인증 설정
- [ ] Clerk 계정 생성: https://clerk.com
- [ ] 프로젝트 생성 후 키 복사
- [ ] `.env`에 환경변수 추가:
  - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_...`
  - `CLERK_SECRET_KEY=sk_...`
- [ ] Clerk 대시보드에서 Google OAuth provider 활성화
- [ ] 허용 이메일 도메인/목록 설정
- [ ] 팀원 초대
- [ ] Clerk 동작 확인 후 `.env`에서 `SITE_PASSWORD` 제거 가능
  - 참고: Clerk 키 없으면 기존 password 인증으로 자동 폴백됨

### 기존 auth 정리 (Clerk 전환 완료 후)
- [ ] `src/app/api/auth/login/route.ts` 삭제 (선택)
- [ ] `src/app/login/page.tsx` Clerk `<SignIn />` 컴포넌트로 교체 (선택)

### 검증
- [ ] Clerk 키 설정 → 로그인 → Clerk 대시보드에서 사용자 활동 확인
- [ ] Prisma query logging으로 N+1 해결 확인

---

## Sprint 4: 인프라 보강

### Inngest 설정
- [ ] Inngest 계정 생성: https://inngest.com
- [ ] `.env`에 환경변수 추가:
  - `INNGEST_EVENT_KEY=...`
  - `INNGEST_SIGNING_KEY=...`
- [ ] 배포 후 `/api/inngest` 엔드포인트 접근 → Inngest 대시보드에서 함수 등록 확인
- [ ] Inngest 대시보드에서 cron 스케줄 정상 동작 확인
- [ ] 안정화 후 기존 `api/jobs/process` Vercel Cron 제거 가능
  - 참고: 기존 job queue(`queue.ts`)와 Inngest가 동시 사용 가능 (점진적 전환)

### Cloudflare R2 설정
- [ ] Cloudflare 대시보드 > R2 > 버킷 생성 (`web-magazine-studio`)
- [ ] API Token 생성 (S3 호환)
- [ ] `.env`에 환경변수 추가:
  - `R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com`
  - `R2_ACCESS_KEY=...`
  - `R2_SECRET_KEY=...`
  - `R2_BUCKET=web-magazine-studio`
  - `R2_PUBLIC_URL=https://assets.yourdomain.com` (선택, 커스텀 도메인)
- [ ] 업로드 테스트 → R2 대시보드에서 파일 확인
  - 참고: R2 미설정 시 기존 로컬 파일 저장으로 자동 폴백

### GitHub Actions CI
- [ ] `.github/workflows/ci.yml` 이미 생성됨
- [ ] PR 생성하여 GitHub Actions 통과 확인
- [ ] 필요 시 GitHub repo Settings > Secrets에 환경변수 추가

### 검증
- [ ] Inngest 대시보드에서 함수 실행 로그 확인
- [ ] R2 업로드 후 URL 접근 가능 확인
- [ ] GitHub Actions CI 통과 확인

---

## 환경변수 전체 목록 (`.env`)

### 필수 (없으면 앱 동작 불가)

| 변수 | 용도 | 발급처 | 비용 |
|------|------|--------|------|
| `DATABASE_URL` | PostgreSQL 연결 | Supabase / Neon / 자체 DB | 무료 티어 |
| `OPENAI_API_KEY` | GPT 호출 (LLM 핵심) | https://platform.openai.com/api-keys | 종량제 |

### 핵심 기능 (없으면 해당 기능 비활성화)

| 변수 | 용도 | 발급처 | 비용 |
|------|------|--------|------|
| `YOUTUBE_API_KEY` | YouTube 트렌드 | Google Cloud Console > APIs > YouTube Data API v3 | 무료 (10K units/day) |
| `NAVER_CLIENT_ID` | Naver 검색/DataLab 트렌드 | https://developers.naver.com/apps | 무료 (25K/day) |
| `NAVER_CLIENT_SECRET` | Naver API 인증 | 위와 동일 | 무료 |
| `SPOTIFY_CLIENT_ID` | 음악 Knowledge Graph | https://developer.spotify.com/dashboard | 무료 |
| `SPOTIFY_CLIENT_SECRET` | Spotify API 인증 | 위와 동일 | 무료 |
| `FAL_KEY` | Flux 이미지 생성 | https://fal.ai/dashboard/keys | 종량제 |
| `SNS_TOKEN_ENCRYPTION_KEY` | SNS 토큰 암호화 (32바이트 hex) | 직접 생성: `openssl rand -hex 32` | - |
| `CRON_SECRET` | Vercel Cron 인증 | 직접 생성: `openssl rand -hex 16` | - |
| `SITE_PASSWORD` | 간이 비밀번호 인증 (Clerk 전환 시 제거) | 직접 설정 | - |

### 웹 검색 (우선순위 순 폴백, 하나만 있어도 됨)

| 변수 | 용도 | 발급처 | 비용 |
|------|------|--------|------|
| `BRAVE_SEARCH_API_KEY` | Brave 웹 검색 (1순위) | https://brave.com/search/api | 무료 (2K/month) |
| `TAVILY_API_KEY` | Tavily 웹 검색 (2순위) | https://tavily.com | 무료 (1K/month) |

### SNS 연동

| 변수 | 용도 | 발급처 | 비용 |
|------|------|--------|------|
| `META_APP_SECRET` | Meta Webhook 서명 검증 | Meta Developer Console | 무료 |
| `META_WEBHOOK_VERIFY_TOKEN` | Meta Webhook 구독 인증 | 직접 설정 | - |
| `OAUTH_CALLBACK_BASE_URL` | SNS OAuth 콜백 URL | 직접 설정 (예: `https://yourdomain.com`) | - |

### 디자인 (선택)

| 변수 | 용도 | 발급처 | 비용 |
|------|------|--------|------|
| `UNSPLASH_ACCESS_KEY` | Unsplash 이미지 검색 | https://unsplash.com/developers | 무료 (50/hour) |
| `PEXELS_API_KEY` | Pexels 이미지 검색 | https://www.pexels.com/api | 무료 |

### Remotion 영상 렌더 (선택)

| 변수 | 용도 | 발급처 | 비용 |
|------|------|--------|------|
| `REMOTION_AWS_REGION` | AWS Lambda 리전 | AWS Console | 종량제 |
| `REMOTION_FUNCTION_NAME` | Lambda 함수명 | Remotion deploy 시 생성 | 종량제 |
| `REMOTION_SERVE_URL` | 번들 URL | Remotion deploy 시 생성 | 종량제 |

### Sprint 1~4 신규 추가

| 변수 | Sprint | 용도 | 발급처 | 비용 |
|------|--------|------|--------|------|
| `SLACK_WEBHOOK_URL` | 1 | 장애 알림 | Slack > 앱 관리 > Incoming Webhook | 무료 |
| `SENTRY_DSN` | 2 | 에러 추적 | https://sentry.io > 프로젝트 생성 | 무료 (5K/month) |
| `LOG_LEVEL` | 2 | 로그 레벨 (info/debug/warn) | 직접 설정 | - |
| `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` | 3 | 팀 인증 (프론트) | https://clerk.com > 프로젝트 | 무료 (10K MAU) |
| `CLERK_SECRET_KEY` | 3 | 팀 인증 (서버) | 위와 동일 | 무료 |
| `INNGEST_EVENT_KEY` | 4 | Job Queue 이벤트 | https://inngest.com > 프로젝트 | 무료 (25K steps/month) |
| `INNGEST_SIGNING_KEY` | 4 | Inngest 서명 검증 | 위와 동일 | 무료 |
| `R2_ENDPOINT` | 4 | 파일 스토리지 엔드포인트 | Cloudflare > R2 | 무료 (10GB) |
| `R2_ACCESS_KEY` | 4 | R2 접근 키 | Cloudflare > R2 > API Token | 무료 |
| `R2_SECRET_KEY` | 4 | R2 비밀 키 | 위와 동일 | 무료 |
| `R2_BUCKET` | 4 | R2 버킷명 | 직접 설정 (예: `web-magazine-studio`) | - |
| `R2_PUBLIC_URL` | 4 | R2 퍼블릭 URL (선택) | Cloudflare 커스텀 도메인 | - |

### 비용 요약

- **고정 비용:** $0/month (모든 외부 서비스 무료 티어)
- **종량제:** OpenAI API (LLM), FAL (이미지), Remotion (영상) — 사용량에 따라

---
